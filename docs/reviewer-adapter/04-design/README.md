# Design

**PARKED, not started.** Triggers when capture count nears 50 (currently 4 —
see the phase gate in [`REVIEWER_ADAPTER_GUIDE.md`](../../REVIEWER_ADAPTER_GUIDE.md)).
This is the dataset schema + validator design for the eventual
`validate_review_dataset.py`. See [00-index.md](../00-index.md) for why the
project paused and how to resume it.
