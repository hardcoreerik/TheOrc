Steps 1 and 2 (GraphModels, GraphRepository, Migration v5, RoslynIndexer, GraphTools/graph_search) are approved and green. Proceed with Step 3 as described in CodeGraph_v1.md:

3. ComplexityAnalyzer + trace_path + get_architecture tools

Specifically:
- Create OrchestratorIDE/Services/CodeGraph/ComplexityAnalyzer.cs
  - Use Roslyn ControlFlowGraph.Create(method) for cyclomatic + loop nesting
  - Cognitive complexity via syntax walk
  - linear_scan_in_loop: detect find/Contains/IndexOf/Where calls inside a loop body
  - transitive_loop_depth: second pass over CALLS edges (max over callees), computed after all nodes exist in GraphRepository
  - is_recursive: node has a CALLS edge to itself

- Add trace_path tool to GraphTools.cs
  - Args: qualified_name, direction=both, depth=3, mode=calls|data_flow
  - Returns caller/callee tree to given depth using GraphRepository.TraceCallers/TraceCallees
  - Plain-text indented tree output

- Add get_architecture tool to GraphTools.cs
  - Args: none
  - Returns: namespace/package list, top-degree hub nodes (top 10 by degree), route list (Route label nodes), total file/node/edge counts from GraphRepository

- Wire ComplexityAnalyzer into RoslynIndexer: after building nodes, run complexity analysis and update graph_nodes rows via GraphRepository.UpdateComplexity(...)

Constraints (hard rules from SKILL.md):
- No new NuGet packages
- AGPL-3.0-or-later + SPDX header on every new file
- Run grok-review on this increment before finishing
- dotnet test OrchestratorIDE.UnitTests must stay green
