# TheOrc / OrchestratorIDE — Agent Instructions

> **This file is a pointer, not the source.**
> The canonical agent briefing lives at [`/.agents.md`](../.agents.md) in the
> repo root. Read that file — it holds the full cross-tool conventions and
> architecture briefing for any AI agent working in this repo (Claude, Codex,
> Grok, or otherwise).
>
> This stub exists only so tools that look under `.claude/` are redirected to
> the single canonical copy. Do not duplicate content here; edit `/.agents.md`.
